# ANA — 2026-09-08 — Login dev, parentescos y documentos

## Login vendedor en desarrollo

`POST /api/auth/vendedor/login-dev` `{ cellphone }`. Solo con
`NODE_ENV=development`. Sesión de vendedor sin PIN.

## Parentescos

`GET /odoo/parentescos` (catálogo Odoo). El front lo usa en captura.

## Documentos nuevos

Kinds: `CARTA_EXCLUSIONES`, `REGLAMENTO_FOLLETO`, `INE` (PDF combinado
frente+reverso). Van a Drive y al mapper del expediente.

`sale_order_reception` 2.02 (campos de archivo en expediente).
